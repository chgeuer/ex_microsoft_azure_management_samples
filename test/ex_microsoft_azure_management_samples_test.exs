defmodule ExMicrosoftAzureManagementSamplesTest do
  use ExUnit.Case
  doctest ExMicrosoftAzureManagementSamples

  test "greets the world" do
    assert ExMicrosoftAzureManagementSamples.hello() == :world
  end
end
